@using AryTickets.Models
@model List<AryTickets.Models.Booking>
@{
    ViewData["Title"] = "Bookings";
    Layout = "_Layout";
    var totalRevenue = Model?.Sum(b => b.TotalPrice) ?? 0;
    var totalBookings = Model?.Count ?? 0;
}

@section Styles {
    @await Html.PartialAsync("_AdminStyles")
}

@await Html.PartialAsync("_AdminSidebar")

<div class="admin-main">
    <div class="flex items-end justify-between mb-10 flex-wrap gap-3">
        <div>
            <span class="text-[11px] font-sans tracking-[0.3em] uppercase text-gold-400">Box Office</span>
            <h1 class="font-serif text-4xl font-semibold text-ivory-100 mt-1">Bookings</h1>
            <p class="font-garamond italic text-ivory-300/70 mt-1">All issued theatre tickets</p>
        </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-5 mb-10">
        <div class="stat-card">
            <p class="text-[10px] font-sans tracking-[0.2em] uppercase text-gold-400">Total bookings</p>
            <p class="font-serif text-4xl font-semibold text-ivory-100 mt-2">@totalBookings.ToString("N0")</p>
        </div>
        <div class="stat-card">
            <p class="text-[10px] font-sans tracking-[0.2em] uppercase text-gold-400">Total revenue</p>
            <p class="font-serif text-4xl font-semibold text-gold-400 mt-2">@totalRevenue.ToString("N2") EUR</p>
        </div>
    </div>

    <div class="panel overflow-hidden">
        @if (Model != null && Model.Count > 0)
        {
            <div class="overflow-x-auto">
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Viewer</th>
                            <th>Production</th>
                            <th>Performance</th>
                            <th>Seats</th>
                            <th>Total</th>
                            <th>Date</th>
                            <th class="text-right">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var b in Model)
                        {
                            <tr>
                                <td class="text-gold-500/60 font-mono text-sm">#@b.Id</td>
                                <td>
                                    <p class="font-serif text-ivory-100">@b.UserName</p>
                                    <p class="text-[11px] font-sans text-gold-500/60 mt-0.5">@b.UserEmail</p>
                                </td>
                                <td class="font-serif">@b.ProductionTitle</td>
                                <td class="font-garamond italic">@b.PerformanceDateTime</td>
                                <td><span class="inline-flex px-2 py-0.5 rounded-sm bg-burgundy-800/40 text-[12px] font-mono text-gold-300">@b.Seats</span></td>
                                <td class="font-serif text-gold-400">@b.TotalPrice.ToString("N2") EUR</td>
                                <td class="text-[12px] text-gold-500/70">@b.BookedAt.ToString("dd MMM yyyy · HH:mm")</td>
                                <td class="text-right">
                                    <form asp-action="DeleteBooking" asp-controller="Admin" method="post" class="inline" onsubmit="return confirm('Are you sure you want to delete this booking?');">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@b.Id" />
                                        <button type="submit" class="admin-btn-danger">Delete</button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center py-16">
                <p class="font-serif text-xl text-ivory-100 mb-2">No bookings yet</p>
                <p class="font-garamond italic text-ivory-300/60">Bookings will appear here once tickets start selling.</p>
            </div>
        }
    </div>
</div>
